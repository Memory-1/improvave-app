name: improv_ave
description: An improviser's handbook.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.1.0

environment:
  sdk: ^3.1.1

dependencies:

  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0 
  table_calendar: ^3.1.2
  youtube_player_iframe: ^5.2.1
  youtube_player_iframe_web: ^3.1.1
  web: ^1.0.0
  webview_flutter: ^4.10.0
  # url_launcher: ^6.3.1

  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:     
         - assets/images/BL_logo.png
         - assets/games.json
